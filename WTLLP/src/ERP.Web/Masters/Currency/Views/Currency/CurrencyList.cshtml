@model ERP.Domain.Entities.CurrencyDetails
@using NonFactors.Mvc.Grid;

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Currency List";
}
<p>
    @Html.ActionLink("Create New Currency", "CreateCurrency", "Currency", "", new { @class = "btn btn-info" }) &nbsp;&nbsp;&nbsp; Search Currency &nbsp;&nbsp;<input class="grid-search" />
</p>
@Html.AjaxGrid(Url.Action("CurrencyList_Partial"))

@section Scripts {
    <script type="text/javascript">

        $(document).on('keyup', '.grid-search', function () {
            //alert("query");
            $('.mvc-grid').mvcgrid({
                query: 'search=' + this.value,
                reload: true
            });

        });
    </script>
}